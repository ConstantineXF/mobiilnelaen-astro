/**
 * Даты реального обновления страниц → honest `lastmod` в sitemap (гид §19).
 *
 * ⛔ Правило: дата ставится ПО ФАКТУ правки контента страницы. Не «сегодня для
 * всего сайта» и не автоматический `new Date()` при каждой сборке — косметически
 * свежий lastmod не даёт эффекта и подрывает доверие краулера.
 * Vercel собирает из shallow-клона, поэтому вытащить дату из git на сборке
 * нельзя — список ведётся здесь руками вместе с правкой контента.
 *
 * Меняешь текст страницы → меняешь её дату в этой таблице, в том же коммите.
 */
export const PAGE_UPDATED = {
  // 04.08.2026 — прогон по §20: слот «что делать при отказе» на всех страницах
  // кластера, снятие выдуманных отзывов/счётчиков с главной.
  '/': '2026-08-04',
  '/kiirlaen/': '2026-08-04',
  '/vaikelaen/': '2026-08-04',
  '/sms-laen/': '2026-08-04',
  '/autolaen/': '2026-08-04',
  '/krediidikonto/': '2026-08-04',
  '/krediidiliin/': '2026-08-04',
  '/krediitkaart/': '2026-08-04',
  // 01.08.2026 — созданы/последний раз правились тогда, сегодня не тронуты.
  '/meist/': '2026-08-01',
  '/metoodika/': '2026-08-01',
  '/kontakt/': '2026-08-01',
  '/privaatsuspoliitika/': '2026-08-01',
  '/kasutustingimused/': '2026-08-01',
};

/** Дата обновления для URL (или null, если страница в таблице не значится). */
export function lastmodFor(pathname) {
  const p = pathname.endsWith('/') ? pathname : pathname + '/';
  return PAGE_UPDATED[p] ?? null;
}
